import { Card } from "./card";

export class Cards {
    brobnarCards: Card[];
    disCards: Card[];
    logosCards: Card[];
    marsCards: Card[];
    sanctumCards: Card[];
    shadowsCards: Card[];
    untamedCards: Card[];
    allCards: Card[];
}